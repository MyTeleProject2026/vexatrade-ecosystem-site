name: VexaTrade Ecosystem Android packages
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  android:
    runs-on: ubuntu-latest
    env:
      USER_KEYSTORE_PATH: ${{ runner.temp }}/vexa-ecosystem-keystores/user.jks
      USER_KEYSTORE_PASSWORD: ${{ secrets.VEXATRADE_ECOSYSTEM_USER_KEYSTORE_PASSWORD }}
      USER_KEY_ALIAS: ${{ secrets.VEXATRADE_ECOSYSTEM_USER_KEY_ALIAS }}
      USER_KEY_PASSWORD: ${{ secrets.VEXATRADE_ECOSYSTEM_USER_KEY_PASSWORD }}
      ADMIN_KEYSTORE_PATH: ${{ runner.temp }}/vexa-ecosystem-keystores/admin.jks
      ADMIN_KEYSTORE_PASSWORD: ${{ secrets.VEXATRADE_ECOSYSTEM_ADMIN_KEYSTORE_PASSWORD }}
      ADMIN_KEY_ALIAS: ${{ secrets.VEXATRADE_ECOSYSTEM_ADMIN_KEY_ALIAS }}
      ADMIN_KEY_PASSWORD: ${{ secrets.VEXATRADE_ECOSYSTEM_ADMIN_KEY_PASSWORD }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v4
      - name: Install Android SDK
        run: sdkmanager "platform-tools" "platforms;android-36" "build-tools;35.0.0"
      - name: Restore release keystores
        env:
          USER_KEYSTORE_BASE64: ${{ secrets.VEXATRADE_ECOSYSTEM_USER_KEYSTORE_BASE64 }}
          ADMIN_KEYSTORE_BASE64: ${{ secrets.VEXATRADE_ECOSYSTEM_ADMIN_KEYSTORE_BASE64 }}
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/vexa-ecosystem-keystores"
          printf '%s' "$USER_KEYSTORE_BASE64" | base64 --decode > "$USER_KEYSTORE_PATH"
          printf '%s' "$ADMIN_KEYSTORE_BASE64" | base64 --decode > "$ADMIN_KEYSTORE_PATH"
          chmod 600 "$USER_KEYSTORE_PATH" "$ADMIN_KEYSTORE_PATH"
          keytool -list -keystore "$USER_KEYSTORE_PATH" -storepass "$USER_KEYSTORE_PASSWORD" -alias "$USER_KEY_ALIAS" >/dev/null
          keytool -list -keystore "$ADMIN_KEYSTORE_PATH" -storepass "$ADMIN_KEYSTORE_PASSWORD" -alias "$ADMIN_KEY_ALIAS" >/dev/null
      - name: Build user APK/AAB
        working-directory: packaging/android
        run: |
          chmod +x ./gradlew
          ./gradlew assembleUserRelease bundleUserRelease
      - name: Build admin APK/AAB
        working-directory: packaging/android
        run: |
          chmod +x ./gradlew
          ./gradlew assembleAdminRelease bundleAdminRelease
      - name: Verify release binaries
        working-directory: packaging/android
        run: |
          set -euo pipefail
          for pattern in 'app/build/outputs/apk/user/release/*.apk' 'app/build/outputs/apk/admin/release/*.apk' 'app/build/outputs/bundle/userRelease/*.aab' 'app/build/outputs/bundle/adminRelease/*.aab'; do
            matches=( $pattern ); [ -e "${matches[0]}" ] || { echo "Missing release binary: $pattern" >&2; exit 1; }
          done
          find app/build/outputs -type f \( -name '*.apk' -o -name '*.aab' \) -print -exec stat -c '%n %s bytes' {} \;
          while IFS= read -r f; do size=$(stat -c '%s' "$f"); [ "$size" -gt 100000 ] || { echo "Release binary unexpectedly small: $f ($size bytes)" >&2; exit 1; }; done < <(find app/build/outputs -type f \( -name '*.apk' -o -name '*.aab' \))
      - uses: actions/upload-artifact@v7
        with:
          name: vexatrade-ecosystem-android-release
          if-no-files-found: error
          path: |
            packaging/android/app/build/outputs/apk/user/release/*.apk
            packaging/android/app/build/outputs/apk/admin/release/*.apk
            packaging/android/app/build/outputs/bundle/userRelease/*.aab
            packaging/android/app/build/outputs/bundle/adminRelease/*.aab
